# Техническое задание
## Проект: визуальный редактор контента на Vue 3 + TypeScript + Vite + Editor.js

## 1. Назначение документа

Данный документ фиксирует требования к новому проекту визуального редактора контента на базе **Vue 3 Composition API**, **TypeScript**, **Vite** и **Editor.js**. Документ предназначен для реализации проекта вручную и с помощью AI-агентов.

Документ описывает:

- целевую архитектуру проекта;
- требования к структуре кода и SCSS;
- поэтапный порядок реализации;
- требования к проверке и критериям готовности на каждом этапе;
- спорные и неоднозначные моменты архитектуры и допустимые варианты решений.

Документ должен использоваться как основной ориентир при разработке первой версии проекта и при его последующем расширении.

---

## 2. Цель проекта

Необходимо реализовать фронтенд-проект редактора контента, в котором:

1. Контент редактируется в Editor.js.
2. Результат сохраняется в JSON-формате.
3. JSON может быть просмотрен на отдельной preview/render-странице.
4. На ранних этапах проект работает **без бэкенда**.
5. В дальнейшем архитектура должна позволять без болезненной перестройки добавлять:
   - кастомные block tools;
   - кастомные inline tools;
   - block tunes;
   - nested editors;
   - валидацию;
   - upload-интеграции;
   - административную обвязку.

Главный принцип проекта: **сначала должен быть реализован стабильный базовый редактор на стандартных инструментах, затем — расширения и кастомизация**.

---

## 3. Цели первой версии

Первая версия проекта должна решать следующие задачи:

- поднимать рабочий Editor.js в Vue-приложении;
- подключать стандартные block tools;
- подключать стандартные inline tools;
- сохранять результат в JSON;
- подставлять начальные данные из статического JSON или из localStorage;
- рендерить сохранённый контент на отдельной странице;
- поддерживать экспорт и импорт JSON;
- иметь архитектурную основу для последующего роста.

---

## 4. Нецели первой версии

На первом этапе не требуется реализовывать:

- полноценный бэкенд;
- настоящий upload файлов на сервер;
- сложную валидацию;
- input masks;
- сложную систему ролей или авторизации;
- окончательную production-полировку UI;
- все возможные кастомные плагины сразу.

Эти задачи относятся к поздним этапам и должны добавляться только после того, как будут стабильно работать базовые редакторные сценарии.

---

## 5. Технологический стек

### 5.1. Базовый стек

Обязательный стек:

- **Vue 3**
- **Composition API**
- **TypeScript**
- **Vite**
- **Editor.js**
- **SCSS**

### 5.2. Поздние зависимости

Допускается подключение на поздних этапах:

- `zod` — для валидации данных;
- `imask` — для input mask;
- дополнительных official и community plugins Editor.js.

---

## 6. Общие архитектурные принципы

### 6.1. Принцип развития архитектуры

Архитектура должна быть заложена **с самого начала**, но без преждевременного усложнения.

Это означает:

- на старте проект должен иметь правильное разделение по слоям;
- тяжёлые абстракции не должны внедряться заранее, если их необходимость ещё не подтверждена;
- архитектура должна расширяться по мере появления реальной сложности.

### 6.2. Приоритет реализации

Приоритет должен быть следующим:

1. Архитектурный каркас.
2. Стандартные block tools.
3. Стандартные inline tools.
4. Сохранение и рендер JSON.
5. Базовые tunes.
6. Простые custom blocks.
7. Nested editors.
8. Сложные кастомные блоки.
9. Кастомные inline tools.
10. Валидация и masks.
11. Некритичные улучшения.

### 6.3. Общий подход к кастомизации

Кастомизация должна появляться только после того, как:

- понятна базовая модель JSON,
- стабильно работает save/load,
- проверена совместимость стандартных block tools и inline tools,
- готов renderer для стандартных блоков.

---

## 7. Требования к структуре проекта

Рекомендуемая структура проекта:

```text
src/
  content/
    default-page.json

  editor/
    shared/
      types/
      entities/
      blocks/
      registry/
      constants/

    admin/
      core/
      config/
      tools/
        blocks/
        inline/
      tunes/
      fields/
      helpers/
      styles/

    renderer/
      components/
        blocks/
      helpers/
      types/

  pages/
    EditorPage/
    PreviewPage/

  components/
  composables/
  utils/
  styles/
```

### 7.1. Смысловое разделение слоёв

#### `editor/shared`
Содержит всё, что используется и в editor-layer, и в renderer-layer:

- типы данных;
- типы блоков;
- registry блоков;
- общие сущности;
- общие константы;
- JSON-модели.

#### `editor/admin`
Содержит всё, что относится именно к редактированию:

- создание editor config;
- block tools;
- inline tools;
- tunes;
- field builders;
- helpers для nested editors;
- инфраструктуру редактора.

#### `editor/renderer`
Содержит всё, что относится к рендеру JSON:

- Vue-компоненты блоков;
- mapping `block.type -> component`;
- renderer utilities;
- типы renderer props.

---

## 8. Требования к SCSS-архитектуре

### 8.1. Основной подход

Все Vue-компоненты должны в первую очередь стилизоваться через **модульные SCSS-файлы**.

Формат подключения стилей должен быть следующим:

```vue
<style module lang="scss" src="./TwoColumns.module.scss" />
```

Этот подход обязателен для:

- обычных Vue-компонентов;
- renderer-компонентов блоков;
- страниц;
- editor-shell компонентов, если они реализованы как Vue-компоненты.

### 8.2. Подключение глобальных SCSS-зависимостей

Необходимо использовать глобальное подключение SCSS entrypoint через `vite.config.ts`, чтобы переменные, миксины, функции, media helpers и общие SCSS-модули не импортировались вручную в каждом `.module.scss`-файле.

Следует исходить из сценария, в котором глобальный SCSS entrypoint подмешивается через `additionalData`.

### 8.3. Структура SCSS

За основу следует взять полноценную структуру SCSS-слоя с разделением на:

- `styles/utils/`
- `styles/utils/typography/`
- `styles/components/`
- `styles/_main.scss`
- `styles/_global.scss`
- `styles/common.scss`
- `styles/css-variables.scss`
- `styles/utilities.scss`

### 8.4. Рекомендуемые SCSS-слои

#### Глобальный SCSS-слой
Должен содержать:

- переменные;
- функции;
- миксины;
- media helpers;
- CSS variables;
- общие utilities;
- общие reset/common правила.

#### Модульный SCSS-слой
Должен использоваться для стилизации конкретных Vue-компонентов.

#### Editor-specific глобальный слой
Допускается наличие отдельного файла наподобие `editor-renderer.scss` и/или `editor-admin.scss` **на всякий случай**, если для некоторых editor-related сущностей предпочтительнее использовать глобальные классы вместо модульных.

Важно:

- этот слой не является основным способом стилизации renderer-компонентов;
- renderer-компоненты всё равно должны в первую очередь использовать module SCSS;
- отдельный глобальный editor-specific слой допускается только как вспомогательный инфраструктурный слой.

### 8.5. Анимации и интеракции

Необходимо принять единое правило: все базовые интеракции и анимации должны иметь длительность **0.3s**.

Это относится к:

- hover-состояниям;
- toggle-переключателям;
- открытию/закрытию select;
- подсветке фокуса;
- появлению ошибок;
- служебным editor UI-переходам.

Следует использовать единый токен перехода в SCSS.

---

## 9. Требования к модели данных

### 9.1. Registry подход

В проекте должен использоваться единый registry блоков.

Registry должен быть единым источником истины для:

- editor-layer;
- renderer-layer;
- типизации блока по `type`;
- типизации `data` конкретного блока;
- типизации renderer props.

### 9.2. Discriminated union

Все блоки должны быть представлены как discriminated union по `type`.

Необходимо использовать общий тип блока, выводимый из registry.

### 9.3. Разделение типов

Не допускается сваливание всех типов в один большой файл.

Типы должны быть разделены минимум на:

- editor output types;
- registry types;
- block data types;
- entity types.

### 9.4. JSON-модель

В качестве основной модели editor output необходимо использовать JSON-структуру, совместимую с `OutputData` Editor.js.

Допустимо иметь свои alias-типы, но не следует создавать вторую конкурирующую модель без необходимости.

---

## 10. Источник данных и хранение

### 10.1. Источник данных по умолчанию

Необходимо использовать статический JSON-файл проекта:

- `src/content/default-page.json`

### 10.2. Draft-хранение

Во время редактирования актуальный черновик должен сохраняться в `localStorage`.

### 10.3. Приоритет источников данных

При открытии редактора и preview необходимо использовать следующий приоритет:

1. Draft из `localStorage`, если он существует.
2. `default-page.json`, если draft отсутствует.

### 10.4. Export / Import

Необходимо реализовать:

- `Export JSON`
- `Import JSON`
- `Reset draft`

Кнопка `Export JSON` должна быть неактивной, если отсутствует актуальный локальный черновик или отсутствуют изменения.

### 10.5. Браузерная запись в проектный JSON

Браузер не должен пытаться напрямую обновлять JSON-файл проекта как обычный файл исходного кода. Обновление проектного JSON должно происходить вручную через экспорт и последующее сохранение в репозиторий.

---

## 11. Требования к editor-layer

### 11.1. Editor config

Необходимо реализовать фабрику конфигурации редактора, например `createEditorConfig`, которая:

- принимает стартовые данные;
- подключает block tools;
- подключает inline tools;
- подключает tunes;
- задаёт `onChange`;
- возвращает единый конфиг Editor.js.

### 11.2. Базовый editor-shell

Необходимо реализовать editor-shell на Vue, который:

- создаёт и уничтожает Editor.js instance;
- получает initial data;
- обрабатывает save/update;
- показывает служебные действия редактора.

### 11.3. Прямое использование `window.EditorJS`

Необходимо избегать зависимости от глобального `window.EditorJS`, если используется модульная установка через npm.

### 11.4. Инициализация без `setTimeout`-костылей

Необходимо стремиться к предсказуемому lifecycle.

Если вложенные editor instances потребуются позже, их инициализация должна строиться на явном lifecycle-менеджменте, а не на случайных `setTimeout`.

---

## 12. Стандартные block tools: обязательный первый функциональный этап

### 12.1. Цель этапа

Поднять рабочий Editor.js на стандартных блоках и получить корректный JSON output.

### 12.2. Базовый набор block tools

На старте должны быть подключены:

- Paragraph
- Header
- List
- Image
- Quote
- Delimiter
- Table
- Embed

### 12.3. Что необходимо проверить

После реализации этапа необходимо проверить:

- редактор успешно инициализируется;
- все подключённые block tools отображаются;
- блоки можно добавлять, редактировать, удалять и перемещать;
- `save()` возвращает валидный JSON;
- сохранённый JSON можно повторно загрузить в editor;
- типы `type` в output совпадают с ключами tools конфигурации.

### 12.4. Критерии готовности этапа

Этап считается завершённым, если:

- редактор стабильно работает на стандартных блоках;
- save/load проходят без потери данных;
- нет необходимости в кастомных block tools для запуска базовой версии.

---

## 13. Стандартные inline tools: обязательный второй функциональный этап

### 13.1. Цель этапа

Подключить и проверить стандартные inline tools до начала кастомизации.

### 13.2. Базовый набор inline tools

На старте должны быть подключены:

- bold
- italic
- link
- underline
- marker (для фона текста)

`inline code` допускается добавить позже как необязательный инструмент.

### 13.3. Проверка совместимости

После реализации необходимо проверить комбинации:

- bold + italic
- bold + link
- underline + link
- marker + link
- marker + underline
- bold + italic + link
- marker + bold + link

Также необходимо проверить:

- повторное открытие ранее сохранённого контента;
- корректность рендера комбинаций;
- отсутствие потери разметки после повторного `save()`.

### 13.4. Критерии готовности этапа

Этап считается завершённым, если:

- стандартные inline tools работают стабильно;
- базовые комбинации инструментов не ломаются;
- сохранение и повторная загрузка не искажают контент.

---

## 14. Renderer: обязательный ранний этап

### 14.1. Цель этапа

Реализовать отдельный renderer JSON-контента на Vue-компонентах.

### 14.2. Требования к renderer

Renderer должен:

- принимать JSON editor output;
- по `block.type` определять нужный Vue-компонент;
- передавать в компонент типизированные `data`;
- корректно рендерить стандартные block tools.

### 14.3. Стили renderer-компонентов

Renderer-компоненты должны в первую очередь использовать **module SCSS**.

Отдельный глобальный `editor-renderer.scss` допускается оставить как резервный слой, но не как основной способ стилизации renderer.

### 14.4. Проверка

Необходимо проверить:

- корректный рендер всех стандартных block tools;
- совпадение структуры JSON и renderer-модели;
- отсутствие зависимости renderer от editor runtime.

### 14.5. Критерии готовности этапа

Этап считается завершённым, если:

- preview-страница корректно рендерит JSON;
- renderer отделён от editor-layer;
- компоненты renderer используют типизированные данные.

---

## 15. Block Tunes: первый этап кастомизации

### 15.1. Общий подход

Block Tunes должны использоваться для дополнительных метаданных блока, а не для внутренней логики конкретного block tool.

### 15.2. Приоритет tunes

Порядок реализации:

1. `AnchorTune`
2. `SpacingTune`
3. `LabelTune` / `TitleTune`
4. `BackgroundTune` — поздний этап
5. `SpoilerTune` — поздний этап

### 15.3. AnchorTune

Должен хранить значение якоря блока и использоваться для последующего скролла по ссылкам вида `#anchor`.

### 15.4. SpacingTune

Должен быть основным решением для задания верхних/нижних отступов блока.

Отдельный `SpacerBlock` допускается только как возможное позднее дополнительное решение, но не как базовый механизм layout-отступов.

### 15.5. LabelTune / TitleTune

Должен хранить label для навигации по странице и использоваться в будущем для sidebar-навигации.

### 15.6. BackgroundTune

Допускается как поздний tune для выбора фонового режима блока.

### 15.7. SpoilerTune

Допускается как поздний tune для скрываемых/раскрываемых блоков.

### 15.8. Проверка

После реализации каждого tune необходимо проверить:

- сохранение состояния tune;
- повторную загрузку состояния;
- применение данных tune в renderer;
- отсутствие влияния на блоки без tune-data.

### 15.9. Критерии готовности этапа

Tune считается реализованным, если:

- значение можно изменить в editor UI;
- значение сохраняется в JSON;
- renderer корректно использует значение;
- блок без заданного tune продолжает работать штатно.

---

## 16. Поля в custom blocks

### 16.1. Общий подход

В проекте должны поддерживаться два типа полей:

- `plain fields`
- `rich fields`

### 16.2. Plain fields

К plain fields относятся:

- text input
- textarea
- select
- radio group
- toggle
- image/url/file selectors

На первых этапах необходимо реализовать именно plain fields.

### 16.3. Toggle

Checkbox в editor UI должен иметь внешний вид toggle-переключателя, при этом на уровне данных он остаётся обычным boolean.

### 16.4. Rich fields

Rich fields должны быть предусмотрены архитектурно, но могут быть реализованы позднее.

К rich fields относятся:

- nested paragraph field;
- nested header field;
- другие single-purpose nested editors.

### 16.5. Архитектурное требование

Необходимо сразу проектировать API полей так, чтобы в будущем одно и то же логическое поле могло быть реализовано:

- либо как plain field,
- либо как rich field.

### 16.6. Ограничение plain fields

Обычные `input` и `textarea` не должны рассматриваться как носители inline tools. Если полю действительно нужны inline tools, это поле должно быть реализовано как rich field на nested Editor.js.

---

## 17. Первый custom block: только после tunes и plain fields

### 17.1. Цель этапа

Реализовать первый простой custom block без nested editors.

### 17.2. Примеры подходящих блоков

- CTA
- Section intro
- Notice
- Banner
- SpacerBlock (только если действительно понадобится как отдельный блок)

### 17.3. Требования к custom block

Каждый custom block должен иметь:

- собственный data type;
- renderer component;
- tool class;
- save/load сценарий;
- при необходимости — tune support;
- при необходимости — простые поля.

### 17.4. Что проверить

- блок можно создать;
- блок можно удалить;
- блок корректно сохраняет данные;
- блок корректно восстанавливается из JSON;
- renderer умеет его отрисовывать.

### 17.5. Критерии готовности этапа

Этап считается завершённым, если как минимум один custom block полностью проходит цикл `create -> edit -> save -> reload -> render`.

---

## 18. Nested editors: отдельный этап

### 18.1. Общий принцип

Nested editors должны добавляться только после того, как:

- работают plain fields;
- реализованы tunes;
- есть хотя бы один простой custom block;
- отлажен базовый lifecycle editor.

### 18.2. Первый nested editor

Первым должен быть реализован **один простой custom block с одним nested editor**.

Не следует начинать сразу с `TwoColumns`.

### 18.3. Ограничение nested editors

Во вложенном editor instance должен быть жёстко ограничен набор tools.

Примеры:

- только `paragraph`;
- только `header`;
- `paragraph + list`.

### 18.4. Single-purpose nested editors

Необходимо предусмотреть режим nested editor, который используется как почти “одно поле”, например:

- rich paragraph field;
- rich header field.

### 18.5. Header-field стратегия

Следует учитывать, что `Header` tool нативно поддерживает выбор уровня заголовка, поэтому в будущем для rich header field желательно использовать вложенный editor на основе official `Header` tool, а не только комбинацию `input + select`.

На раннем этапе допускается сначала использовать `input + select(level)`, если это ускоряет реализацию.

### 18.6. Ограничение одного блока

Если вложенный editor используется как “одно rich-поле”, необходимо своей логикой ограничивать или валидировать сценарий, в котором допускается только один block.

### 18.7. Проверка

- nested editor корректно создаётся;
- nested editor корректно уничтожается;
- save/load nested data работают стабильно;
- ограниченный whitelist tools соблюдается;
- renderer корректно рендерит вложенный контент.

### 18.8. Критерии готовности этапа

Этап считается завершённым, если существует хотя бы один custom block с корректно работающим single-purpose nested editor.

---

## 19. TwoColumns и другие composite blocks

### 19.1. Порядок

`TwoColumns` и другие сложные composite blocks должны реализовываться только после успешного этапа с одним nested editor.

### 19.2. Требования к TwoColumns

`TwoColumns` должен хранить:

- layout variant;
- direction / reversed state;
- tune data;
- left content;
- right content.

`left` и `right` должны быть представлены как отдельные nested editor outputs.

### 19.3. Разрешённые инструменты во вложенных колонках

Для колонок должен использоваться ограниченный whitelist tools, например:

- paragraph
- header
- list

### 19.4. Проверка

- корректный save/load обеих колонок;
- отсутствие конфликтов toolbar;
- корректный renderer;
- стабильное поведение при удалении и перемещении блока.

---

## 20. Кастомные inline tools

### 20.1. Порядок реализации

Кастомные inline tools должны реализовываться только после стандартных inline tools и после появления хотя бы одного стабильного nested editor сценария.

### 20.2. Приоритет inline tools

Следует реализовать:

1. Стандартный `Marker` для цвета фона текста.
2. Кастомный inline tool для цвета текста.

### 20.3. Общий подход к кастомному inline tool

Кастомный tool для цвета текста должен:

- использовать собственную inline-обёртку;
- использовать собственный CSS class;
- не мутировать существующие `b`, `i`, `a`, `u` и другие inline-элементы;
- корректно сохраняться и восстанавливаться.

### 20.4. Проверка

Необходимо проверить комбинации:

- text color + bold
- text color + italic
- text color + link
- text color + underline
- text color + background marker
- text color + несколько стандартных inline tools одновременно

### 20.5. Критерии готовности этапа

Этап считается завершённым, если кастомный inline tool:

- работает в nested editor и/или основном editor;
- корректно сохраняется;
- корректно рендерится;
- не ломает комбинации со стандартными inline tools.

---

## 21. Архитектура custom tools и helpers

### 21.1. Общий принцип

Необходимо избегать монолитных helper-классов наподобие универсального `EditorFields`, который знает сразу обо всех типах полей, upload, nested editors и логике tool lifecycle.

### 21.2. Предпочтительная стратегия

Следует использовать набор маленьких специализированных модулей:

- builders для plain fields;
- helpers для image/media fields;
- helpers для nested editor shell;
- managers для lifecycle nested editors;
- отдельные helpers для selection/inline logic;
- отдельные helpers для DOM building.

### 21.3. Наследование и композиция

По умолчанию должна использоваться **композиция**, а не глубокая иерархия наследования.

Узкие base classes допустимы позже, если появится реальная повторяемость между custom block tools, inline tools или tunes.

### 21.4. Передача контекста

Не следует строить архитектуру на передаче целого `this` во все helper-классы.

Следует передавать только необходимые зависимости явно.

---

## 22. Проверка после каждого этапа

После каждого этапа необходимо фиксировать:

1. Что было реализовано.
2. Что было проверено вручную.
3. Какие сценарии прошли успешно.
4. Какие ограничения или риски выявлены.
5. Какие решения сознательно отложены на следующий этап.

### 22.1. Обязательные категории проверки

На большинстве этапов нужно проверять:

- инициализацию;
- сохранение;
- повторную загрузку;
- рендер;
- типизацию;
- удаление/перемещение блоков;
- устойчивость при пустых данных;
- отсутствие неочевидных race conditions.

---

## 23. Валидация и masks: поздний этап

### 23.1. Приоритет

Валидация не относится к ранним этапам и должна появляться ближе к завершению базовой редакторной функциональности.

### 23.2. Библиотеки

Допускается использовать:

- `zod` — для схем и декларативной валидации;
- `imask` — для input masks.

### 23.3. Уровни валидации

#### Plain fields
Ошибки должны отображаться под конкретным полем.

#### Nested editors
Ошибки должны отображаться под конкретным nested editor как под цельным полем.

#### Внутренние блоки nested editor
Отдельная адресная валидация внутренних paragraph/header/list block-элементов не является обязательной для первой версии и может быть отложена.

### 23.4. Минимальная валидация

На первом шаге валидации достаточно:

- required;
- min/max length;
- email;
- url;
- phone pattern.

### 23.5. Полная валидация

Более сложная валидация и маски относятся к одному из последних этапов.

### 23.6. Критерии готовности этапа

Этап считается завершённым, если:

- plain fields поддерживают базовые ошибки;
- nested editor fields поддерживают ошибки на уровне поля;
- ошибки отображаются предсказуемо;
- сохранение и экспорт можно ограничить при критичных ошибках.

---

## 24. Медиа на ранних этапах

### 24.1. Изображения

До появления бэкенда необходимо использовать упрощённый подход:

- static files из проекта;
- public assets;
- URL;
- при необходимости временные blob/data URLs.

### 24.2. Видео

На раннем этапе необходимо использовать:

- embed URL;
- статичные mp4/webm из проекта.

### 24.3. Что не требуется

Не требуется реализовывать полноценный upload flow до появления бэкенда.

---

## 25. Спорные и неоднозначные архитектурные вопросы

### 25.1. SpacerBlock vs SpacingTune

#### Предпочтительное решение
Основным механизмом отступов должен быть `SpacingTune`.

#### Допустимая альтернатива
`SpacerBlock` может быть добавлен позже как отдельный технический блок, если редактору действительно понадобится самостоятельный пустой layout-блок.

### 25.2. Plain field vs rich field

#### Предпочтительное решение
По умолчанию следует использовать plain fields.

#### Допустимая альтернатива
Если полю действительно нужны inline tools, допускается rich field на nested Editor.js.

### 25.3. Заголовок как input или как nested header editor

#### Предпочтительное долгосрочное решение
Nested header editor с inline tools и нативным выбором уровня заголовка.

#### Допустимое раннее решение
`input + select(level)` как временный первый этап.

### 25.4. Marker vs custom background tool

#### Предпочтительное решение
Для фона текста следует использовать стандартный `Marker`.

#### Поздняя альтернатива
Кастомный background inline tool может быть добавлен позже, если стандартного Marker окажется недостаточно.

### 25.5. Class vs data-attributes для кастомных inline стилей

#### Предпочтительное решение
Использовать CSS classes.

#### Допустимая альтернатива
`data-*` может быть выбран позднее, если появится необходимость в более абстрактной theme-model.

### 25.6. Степень раннего архитектурного усложнения

#### Предпочтительное решение
Лёгкий каркас с постепенным ростом архитектуры.

#### Что не рекомендуется
Полноценный heavy framework для tools/tunes/helpers до появления реальной сложности.

---

## 26. Последовательность этапов реализации

### Этап 1. Инициализация проекта

Содержимое этапа:

- создание Vue 3 + TS + Vite проекта;
- настройка SCSS и module SCSS;
- подключение глобального SCSS entrypoint через Vite;
- подготовка структуры директорий.

Проверка:

- проект запускается;
- module SCSS работает;
- глобальные SCSS зависимости автоматически доступны.

Критерий готовности:

- создан рабочий каркас фронтенд-проекта.

### Этап 2. Shared-слой и registry

Содержимое этапа:

- типы editor output;
- registry блоков;
- discriminated union;
- общие entities.

Проверка:

- типизация работает;
- shared types используются в editor и renderer.

Критерий готовности:

- есть единая типизированная модель данных.

### Этап 3. Editor shell и источник данных

Содержимое этапа:

- EditorPage;
- PreviewPage;
- default JSON;
- localStorage draft loading.

Проверка:

- editor и preview открываются;
- данные подставляются из нужного источника.

Критерий готовности:

- есть рабочий каркас взаимодействия с данными.

### Этап 4. Стандартные block tools

Содержимое этапа:

- подключение Paragraph/Header/List/Image/Quote/Delimiter/Table/Embed.

Проверка:

- блоки создаются и сохраняются.

Критерий готовности:

- редактор стабильно работает на стандартных блоках.

### Этап 5. Стандартные inline tools

Содержимое этапа:

- подключение bold/italic/link/underline/marker.

Проверка:

- комбинации работают;
- данные не теряются после reload.

Критерий готовности:

- стандартные inline tools стабильны.

### Этап 6. Renderer

Содержимое этапа:

- mapping block type to component;
- renderer-компоненты для стандартных блоков.

Проверка:

- JSON корректно рендерится.

Критерий готовности:

- preview отображает контент, сохранённый редактором.

### Этап 7. Export/Import и draft UX

Содержимое этапа:

- Export JSON;
- Import JSON;
- Reset draft;
- disabled state для Export.

Проверка:

- экспортируется актуальный JSON;
- импорт работает;
- reset очищает локальный draft.

Критерий готовности:

- есть полный UX управления локальным контентом.

### Этап 8. Tunes

Содержимое этапа:

- AnchorTune;
- SpacingTune;
- LabelTune.

Проверка:

- данные tunes сохраняются и рендерятся.

Критерий готовности:

- базовые метаданные блока работают.

### Этап 9. Plain field system

Содержимое этапа:

- text input;
- textarea;
- select;
- toggle;
- radio group;
- field wrappers.

Проверка:

- поля удобно использовать внутри custom blocks.

Критерий готовности:

- есть минимальная reusable система plain fields.

### Этап 10. Первый простой custom block

Содержимое этапа:

- custom block без nested editor.

Проверка:

- полный цикл create/edit/save/reload/render.

Критерий готовности:

- хотя бы один custom block работает end-to-end.

### Этап 11. Первый nested editor block

Содержимое этапа:

- один custom block с одним nested editor.

Проверка:

- nested editor стабилен.

Критерий готовности:

- реализован первый single-purpose rich field.

### Этап 12. Composite blocks

Содержимое этапа:

- TwoColumns и аналогичные блоки.

Проверка:

- многоконтейнерный nested контент сохраняется и рендерится.

Критерий готовности:

- сложные кастомные блоки работают стабильно.

### Этап 13. Кастомный inline tool для цвета текста

Содержимое этапа:

- кастомный text color inline tool.

Проверка:

- совместимость со стандартными inline tools.

Критерий готовности:

- text color tool корректно работает в боевых сценариях.

### Этап 14. Валидация

Содержимое этапа:

- базовые схемы Zod;
- UI ошибок;
- минимальная block validation.

Проверка:

- ошибки отображаются предсказуемо.

Критерий готовности:

- проект поддерживает базовую валидацию plain и rich полей.

### Этап 15. Masks и некритичные улучшения

Содержимое этапа:

- IMask;
- polished interactions;
- поздние tunes;
- sidebar navigation;
- optional enhancements.

Проверка:

- все поздние улучшения не ломают базовый сценарий.

Критерий готовности:

- проект доведён до расширенной версии без разрушения базовой архитектуры.

---

## 27. Финальная позиция по реализации

Проект должен разрабатываться по принципу:

- сначала стабильная база;
- затем расширяемость;
- затем сложные custom scenarios;
- затем polish и поздние улучшения.

Главная цель архитектуры — не максимальное количество abstraction layers с первого дня, а **управляемое масштабирование Editor.js-проекта без хаотичного роста сложности**.
